{
  "name": "flydo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
      "compile": "bun build --compile src/index.ts --outfile dist/flydo"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.4.4-r.1"
  }
}
